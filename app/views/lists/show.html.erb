<%= @list.name %>
<%= cl_image_tag @list.photo.key, height: 300, width: 400, crop: :fill %>
<%= link_to "delete", list_path(@list), method: :delete, data: { confirm: "Are your sure?" }  %>
<%= link_to "Back", root_path %>
